# Spotify
SPOTIFY_CLIENT_ID=38a0d237339244e393f7a95994d36ff0
SPOTIFY_CLIENT_SECRET=762281dd59d64408872d8eb93daaf6f2
SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/auth/callback

# Security
SESSION_SECRET=change_me

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/woody

# Acoustic Analysis Service (packages/acoustic-service)
# Run locally: cd packages/acoustic-service && uvicorn service:app --port 8765
# Or deploy to Modal: cd packages/acoustic-service && modal deploy modal_app.py
# Leave empty to disable acoustic ranking (falls back to pool-order selection)
ACOUSTIC_SERVICE_URL=http://localhost:8765
