"""FastAPI routers for the Woody acoustic service.

embed       — CLAP audio/text encoding (Layer 2 navigation)
arc         — Arc generation via greedy beam search with relaxation
projection  — 5D display projection (Phase 1 heuristic from Spotify features)
"""
