"""One-shot CLI scripts for the acoustic service.

seed_corpus.py    — populate sqlite-vec with ~500 diverse Spotify tracks
                    embedded via CLAP through the running service
test_arc.py       — Step 1.3 listen test (arrives in next commit)
validate_probes.py— Step 2.4 probe span check (arrives in step 2.4)
"""
